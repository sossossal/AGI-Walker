# AGI-Walker Requirements
# Core Dependencies
numpy>=1.24.0
matplotlib>=3.7.0
gymnasium>=0.29.0
stable-baselines3>=2.0.0

# Deep Learning
torch>=2.0.0
transformers>=4.30.0
peft>=0.4.0

# Simulation & Physics
# (Godot 需要单独安装)

# Communication & Networking (NEW - OpenNeuro Integration)
eclipse-zenoh>=0.10.0  # Zenoh 核心库
# rclpy  # ROS 2 Python 客户端 (通过 apt 安装，不在 pip)

# Utilities
opencv-python>=4.8.0  # 用于 Dashboard 图像处理
PyQt6>=6.5.0  # GUI (可选)

# Development Tools
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
