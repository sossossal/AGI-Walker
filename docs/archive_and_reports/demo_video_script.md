# Demo Video Script

This archived page stores a lightweight script outline for a short project demo.

## Suggested Runtime Focus

- Show the repository entry page
- Start the CLI
- Start the Web panel
- Show the MCP tool surface
- End with where to find the current docs

## 60 Second Outline

1. Open the repository and point at `README.md`.
2. Show `python -m agi_walker.cli` as the command-line entry.
3. Show `python -m web_panel.server` as the browser workflow entry.
4. Mention `agi-walker-mcp` as the MCP server entry.
5. Close on the current guides and known archive area.

## Archive Note

Use this as presentation scaffolding only. Update the exact screen flow before recording anything new.
